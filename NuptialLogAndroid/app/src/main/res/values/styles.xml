<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="HeaderText">
        <item name="android:textSize">@dimen/header_text</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="HeaderText.Light" parent="HeaderText">
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="ContentLabel">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ChangelogText">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="ChangelogText.Light" parent="ChangelogText">
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="UserLabel" parent="ContentLabel">
        <item name="android:drawableEnd">@drawable/ic_antbluesmall</item>
        <item name="android:drawablePadding">@dimen/ant_user_spacing</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

</resources>
